# The routes of the project.
#
# Each route describes how an incoming URL is going
# to be processed by Platform.sh.


# "https://blundell.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://blundell.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://bridge.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://bridge.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://brighouse.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://brighouse.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://byng.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://byng.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://cook.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://cook.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://debeck.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://debeck.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://diefenbaker.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://diefenbaker.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://dixon.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://dixon.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://errington.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://errington.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://ferris.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://ferris.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://gardencity.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://gardencity.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://currie.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://currie.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://gilmore.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://gilmore.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://grauer.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://grauer.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://hamilton.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://hamilton.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://homma.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://homma.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://kidd.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://kidd.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://kingswood.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://kingswood.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://lee.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://lee.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://maplelane.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://maplelane.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://mckay.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://mckay.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://mckinney.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://mckinney.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://mcneely.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://mcneely.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://mitchell.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://mitchell.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://quilchena.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://quilchena.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://spulukwuks.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://spulukwuks.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://steves.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://steves.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://tait.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://tait.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://talmey.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://talmey.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://thompson.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://thompson.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://tomsett.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://tomsett.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://westwind.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://westwind.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://whiteside.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://whiteside.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://anderson.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://anderson.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://blair.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://blair.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://elementary.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://woodward.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://woodward.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://wowk.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://wowk.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://boyd.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://boyd.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://hjcambie.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://hjcambie.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://macneill.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://macneill.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://mcmath.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://mcmath.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://mcnair.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://mcnair.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://mcroberts.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://mcroberts.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://palmer.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://palmer.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://slss.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://slss.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://jnburnett.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://jnburnett.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://rhs.{default}/":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://rhs.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://careerprograms.{default}/":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

# "https://ce.{default}/":
#     type: upstream
#     upstream: "app:http"
#     cache:
#         enabled: true
#         # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
#         cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://ce.sd38.bc.ca/":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://rvs.{default}/":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://rvs.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']


"https://training.{default}/":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']


"https://stationstretch.{default}/":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://stationstretch.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://rdpa.{default}/":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']

"https://rdpa.sd38.bc.ca":
    type: upstream
    upstream: "app:http"
    cache:
        enabled: true
        # Base the cache on the session cookie and custom Drupal cookies. Ignore all other cookies.
        cookies: ['/^SS?ESS/', '/^Drupal.visitor/']
